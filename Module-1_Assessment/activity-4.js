//Create an array with 3 student names

const classmate=["Troy","Michelle","Kiyah"];

// in a loop prompt the user for 3 more names
const classmate= prompt('enter the name of another classmate');
classmatelist = '';

for (i = 3; i < classmate.length; i++) {
    
}

//After every user input store the new name into the array
classmatlist.push(classmate)